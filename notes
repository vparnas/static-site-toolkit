# TODO

1. rsync Don't maintain permissions - reset to default, or guarantee ugo+r access
1. ssg BUG: VAR insertion breaks if multiple comment tags on same line
1. ssg: don't process .conf (or non-web) files in web directory
1. ssg: If AUTHOR config var present, always include author (if not superceded by metadata author) BUT only on dated posts?
1. ssg: process template vars in the global footer template.
1. ssg: command line options - redo for optional args, considering the config file
1. gen_content_list: $IGNORE directive in find not working
1. Incorporate the link_cross_postings::fix_links routine into Makefile via implicit variables

# Ideas

1. Leverage a global def list of link definitions
1. configurable to select the category html name format (include extension in link?)
1. X - Templates containing categories: incorporate category links
1. gen_content_list: what date to use for non-markdown? appeal to date set in content file?
1. X - use CFG_FILE vars for output directory, file names, etc

# Completed

1. ssg5: menu generation: don't expect 'nav' field, only the id menu
1. gen_content_list: extract summary here (if not explicitly provided in metadata) instead of the RSS routine, thus enabling the RSS generation from a template as with all other indexes. 
1. Similar to RSS generation, only produce the newer templated entries; leverage the existing.

 vim:set ft=markdown: 
